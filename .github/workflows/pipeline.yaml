name: ci

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize

permissions:
  contents: write
  id-token: write
  issues: write
  packages: write
  pull-requests: write

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ──────────────────────────────────────────────
  # Always-run jobs (PR + main)
  # ──────────────────────────────────────────────
  test:
    uses: platform-mesh/.github/.github/workflows/job-node-test.yml@ab6caea57060a3eba5fc58cadbb2aaa1b06da18a # main
    secrets: inherit

  docker-build:
    if: github.event_name == 'pull_request'
    uses: platform-mesh/.github/.github/workflows/job-docker-build-push.yml@ab6caea57060a3eba5fc58cadbb2aaa1b06da18a # main
    with:
      imageTagName: ghcr.io/platform-mesh/generic-resource-ui
    secrets: inherit

  # ──────────────────────────────────────────────
  # Quality gate (aggregates required checks)
  # ──────────────────────────────────────────────
  quality-gate:
    if: always()
    permissions: {}
    needs: [test, docker-build]
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Check results
        run: |
          if [[ "${{ needs.test.result }}" != "success" ]]; then
            echo "test failed"
            exit 1
          fi
          # docker-build is skipped on main pushes — allow skipped
          if [[ "${{ needs.docker-build.result }}" != "success" && "${{ needs.docker-build.result }}" != "skipped" ]]; then
            echo "docker-build failed"
            exit 1
          fi

  # ──────────────────────────────────────────────
  # Release jobs (main branch only)
  # ──────────────────────────────────────────────
  create-version:
    if: github.ref == 'refs/heads/main'
    uses: platform-mesh/.github/.github/workflows/job-create-version.yml@ab6caea57060a3eba5fc58cadbb2aaa1b06da18a # main
    secrets: inherit

  docker-build-push:
    if: github.ref == 'refs/heads/main'
    needs: [create-version, test]
    uses: platform-mesh/.github/.github/workflows/job-docker-build-push.yml@ab6caea57060a3eba5fc58cadbb2aaa1b06da18a # main
    with:
      imageTagName: ghcr.io/platform-mesh/generic-resource-ui
      version: ${{ needs.create-version.outputs.version }}
      multiarch: true
    secrets: inherit

  update-version:
    if: github.ref == 'refs/heads/main'
    needs: [create-version, docker-build-push]
    uses: platform-mesh/.github/.github/workflows/job-chart-version-update.yml@ab6caea57060a3eba5fc58cadbb2aaa1b06da18a # main
    secrets: inherit
    with:
      appVersion: ${{ needs.create-version.outputs.version }}
      chart: generic-resource-ui
      targetRepository: platform-mesh/helm-charts

  sbom:
    if: github.ref == 'refs/heads/main'
    needs: [create-version, docker-build-push]
    uses: platform-mesh/.github/.github/workflows/job-sbom.yml@ab6caea57060a3eba5fc58cadbb2aaa1b06da18a # main
    with:
      imageReference: ghcr.io/platform-mesh/generic-resource-ui:${{ needs.create-version.outputs.version }}

  image-ocm:
    if: github.ref == 'refs/heads/main'
    needs: [create-version, docker-build-push, sbom]
    uses: platform-mesh/.github/.github/workflows/job-image-ocm.yml@ab6caea57060a3eba5fc58cadbb2aaa1b06da18a # main
    secrets: inherit
    with:
      imageReference: ghcr.io/platform-mesh/generic-resource-ui:${{ needs.create-version.outputs.version }}
      appVersion: ${{ needs.create-version.outputs.version }}
      repoName: generic-resource-ui
      commit: ${{ github.sha }}
